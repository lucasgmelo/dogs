import React from 'react'
import { 

} from './ButtonStyles';

const Button = ({textBtn}) => {
    return (
        <div>
            <button>{textBtn}</button>
        </div>
    )
}

export default Button
